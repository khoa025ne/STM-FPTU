@page
@model FPT_SM.Web.Pages.Admin.Semesters.CreateModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Tạo học kỳ";
}

<!-- Breadcrumb -->
<nav class="flex items-center gap-2 text-sm text-slate-400 mb-6" data-aos="fade-down">
    <a href="/Admin/Semesters" class="hover:text-fpt-navy transition-colors">Quản lý Học kỳ</a>
    <i data-lucide="chevron-right" class="w-4 h-4"></i>
    <span class="text-slate-600 font-medium">Tạo học kỳ</span>
</nav>

<div class="max-w-2xl mx-auto" data-aos="fade-up">
    <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
        <!-- Card Header -->
        <div class="bg-gradient-to-r from-fpt-navy to-blue-800 px-6 py-5">
            <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-white/20 rounded-xl flex items-center justify-center">
                    <i data-lucide="calendar-plus" class="w-5 h-5 text-white"></i>
                </div>
                <div>
                    <h2 class="text-white font-bold text-lg">Tạo học kỳ</h2>
                    <p class="text-white/60 text-sm">Điền đầy đủ thông tin bên dưới</p>
                </div>
            </div>
        </div>

        <!-- Form -->
        <form method="post" class="p-6 space-y-5">
            @Html.AntiForgeryToken()

            <!-- Validation Summary -->
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="bg-red-50 border border-red-200 rounded-xl p-4 flex items-start gap-3">
                    <i data-lucide="alert-circle" class="w-5 h-5 text-red-500 flex-shrink-0 mt-0.5"></i>
                    <div>
                        <p class="text-sm font-semibold text-red-700 mb-1">Vui lòng kiểm tra lại thông tin:</p>
                        <ul class="text-sm text-red-600 space-y-0.5">
                            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                            {
                                <li>• @error.ErrorMessage</li>
                            }
                        </ul>
                    </div>
                </div>
            }

            <!-- Tên học kỳ -->
            <div>
                <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                    Tên học kỳ <span class="text-red-500">*</span>
                </label>
                <input asp-for="CreateDto.Name" type="text" placeholder="Ví dụ: Fall 2025"
                       class="w-full px-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-fpt-navy/20 focus:border-fpt-navy/40 text-sm transition-all" />
                <span asp-validation-for="CreateDto.Name" class="text-xs text-red-500 mt-1 block"></span>
            </div>

            <!-- Mã học kỳ -->
            <div>
                <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                    Mã học kỳ <span class="text-red-500">*</span>
                </label>
                <input asp-for="CreateDto.Code" type="text" placeholder="Ví dụ: FA25"
                       class="w-full px-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-fpt-navy/20 focus:border-fpt-navy/40 text-sm transition-all" />
                <span asp-validation-for="CreateDto.Code" class="text-xs text-red-500 mt-1 block"></span>
            </div>

            <!-- Ngày bắt đầu + Ngày kết thúc -->
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                        Ngày bắt đầu <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="CreateDto.StartDate" type="date"
                           class="w-full px-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-fpt-navy/20 focus:border-fpt-navy/40 text-sm transition-all" />
                    <span asp-validation-for="CreateDto.StartDate" class="text-xs text-red-500 mt-1 block"></span>
                </div>
                <div>
                    <label class="block text-sm font-semibold text-slate-700 mb-1.5">
                        Ngày kết thúc <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="CreateDto.EndDate" type="date"
                           class="w-full px-4 py-2.5 rounded-xl border border-slate-200 focus:outline-none focus:ring-2 focus:ring-fpt-navy/20 focus:border-fpt-navy/40 text-sm transition-all" />
                    <span asp-validation-for="CreateDto.EndDate" class="text-xs text-red-500 mt-1 block"></span>
                </div>
            </div>

            <!-- Divider -->
            <hr class="border-slate-100" />

            <!-- Actions -->
            <div class="flex items-center justify-between pt-1">
                <a href="/Admin/Semesters"
                   class="inline-flex items-center gap-2 px-5 py-2.5 rounded-xl border border-slate-200 text-sm font-semibold text-slate-600 hover:bg-slate-50 transition-colors">
                    <i data-lucide="arrow-left" class="w-4 h-4"></i>
                    Hủy bỏ
                </a>
                <button type="submit"
                        class="inline-flex items-center gap-2 px-6 py-2.5 bg-fpt-orange hover:bg-orange-500 text-white font-bold rounded-xl text-sm transition-all hover:shadow-lg">
                    <i data-lucide="save" class="w-4 h-4"></i>
                    Tạo học kỳ
                </button>
            </div>
        </form>
    </div>
</div>
